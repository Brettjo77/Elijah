# Claude Editing Notes

This project is a no-build static website for GitHub Pages. Keep it simple and beginner friendly.

## Project rules

- Edit `index.html` and `styles.css` directly unless the user asks for a larger framework.
- Keep the site publishable from the repository root on GitHub Pages.
- Do not add npm, Vite, React, or other build tooling unless the user explicitly requests it.
- Place design references from Claude in `design-pack/`.
- Place images in `assets/images/` and reference them with relative paths.
- Preserve mobile responsiveness.
- Keep visible text focused on Elijah Ford's Microsoft experience and the AI-assisted creation process.

## Likely next task

When the design pack arrives, update the visual direction, typography, spacing, and layout to match it while keeping the current static deployment model.